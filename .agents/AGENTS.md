# Custom Rules

- Whenever the user provides an image or file with a generic name (e.g. `Asset 1 1.svg`), ALWAYS rename it to a descriptive name based on the section or component it belongs to (e.g. `header-logo.svg`, `hero-background.jpg`, etc.) before using it in the code.
